using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonsterController : MonoBehaviour
{

    // This class controls sea monsters

    // Monsters will stay still until a ship comes into range
    // Monsters will follow lightbeams but stay slightly out of view if no ships are nearby
    // Monsters will dive back into the water and dissapear after being shown for a extended amount of time

    public static bool inLight;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
